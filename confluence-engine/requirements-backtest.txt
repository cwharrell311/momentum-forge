# Backtesting-only dependencies.
# Used by GitHub Actions workflows — no web server, no database, no Redis.
# Lightweight and fast to install.

numpy>=1.24.0
pandas>=2.0.0
yfinance>=0.2.31
scipy>=1.11.0

# ccxt for crypto exchange data (Binance etc.)
ccxt>=4.0.0

# Alpaca broker (paper + live trading)
alpaca-py>=0.33.0

# Signal processing (optional — wavelet denoising)
PyWavelets>=1.4.0

# HMM regime detection (optional — falls back to efficiency ratio)
hmmlearn>=0.3.0

# Meta-labeling + feature engineering (optional — falls back to numpy logistic regression)
scikit-learn>=1.3.0

# Watchlist config loading
PyYAML>=6.0

# OANDA FX data (optional — only for --fx pairs)
httpx>=0.25.0

# IBKR futures (optional — falls back to ETF proxies without it)
# ib_insync>=0.9.86

# FinBERT NLP on SEC filings (optional — falls back to keyword analysis)
# transformers>=4.35.0
# torch>=2.0.0

# Rust hot path build tool (optional — Python fallback used without it)
# maturin>=1.0

# AI evaluation (optional — only used with --ai-eval flag)
anthropic>=0.43.0
openai>=1.58.1
